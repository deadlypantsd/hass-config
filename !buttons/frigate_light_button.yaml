type: custom:button-card
entity: light.[light_entity]
name: >-
  [[[
    if (states['input_boolean.frigate_[camera_name]_override'].state === 'on') {
      return `Lights(m)`;
    } else {
      return `Lights`;
    }
  ]]]
icon: mdi:light
show_state: false
hold_action:
  action: toggle
  entity: input_boolean.frigate_[camera_name]_override
template:
  - base
  - light
  - icon_light
